  
<h1>List of Places</h1>

{{#each places}}
  <a href="/place/{{ _id }}">
    <h4>{{ name }}</h4>
    <img src="{{ picture }}" />
    <small>Created by {{ creator.name }}</small>
    <small>{{ timestamps }}</small>
  </a>
{{/each}}