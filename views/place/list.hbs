  
<h1>List of Places</h1>

{{#each places}}
  <a href="/place/{{ _id }}">
    <h4>{{ name }}</h4>
  </a>
    <img src="{{ upload.images }}" />
    <small>Created by {{ creator.mail }}</small>
    <small>{{ timestamps.createdAt }}</small>
{{/each}}